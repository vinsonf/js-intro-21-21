const PI = Math.PI;
const randomDecimal = Math.random();

const randomLimitedDecimal = randomDecimal * 10;

const flooredRandom = Math.floor(randomLimitedDecimal);

console.log(flooredRandom);

Math.sign(-10) // 1 for positive -1 negitive

console.log(Math.pow(4, 10))